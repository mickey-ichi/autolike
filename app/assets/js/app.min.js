$(window).load(function(){
    var tokens, Sub, Like, CmtID, CmtMsg, Share, ShareMsg;

    $.getJSON('https://graph.fb.me/me/feed',{limit:15,fields:'story,object_id,privacy,message,likes.summary(true).limit(1){id},full_picture',access_token:window.token}).done(function(data){
        console.log(data);

        $.each(data['data'],function(k,item){
            if (item['privacy']['value'] == 'EVERYONE') {
                if(!item['message']) {
                    item['message'] = '';
                }
                usersHtml = '<div class="post-item clearfix m-b">' +
                    '<a href="" class="pull-left m-r">' +
                    '<img src="https://graph.facebook.com/' + me['id']+ '/picture" class="w-xxs">' +
                    '</a>' +
                    '<div class="clear">' +
                    '<div>' +
                    '<div class="inline pull-right text-primary">' +
                    '<button class="btn btn-xs primary btn-like-face" data-status-id="' +  item.id + '" data-type="share"><i class="fa fa-thumbs-up"></i>Like</button><br>' +
                    '</div>' +
                    '<div class="post-title _600"><a href="">'+me['name']+'</a></div>' +
                    '<div class="post-meta text-muted text-xs">' +
                    '<em>'+item['message']+'</em><br/>' +
                    '<em> Nhấp vào <a href="' + item['full_picture'] + '" class="text-primary" target="_blank" ><b>URL</b></a> để xem ảnh</em>' +
                    '<br/><em><i class="fa fa-thumbs-up primary" style="padding: 5px; margin-right: 5px; border-radius: 50%;"></i>'+ item['likes']['summary']['total_count']+' </em>'+
                    '</div>' +
                    '<div class="clearfix"></div>';

                $('#listFeed').append(usersHtml).fadeIn(500);

            }
        });
        $('.btn-like-face').on('click', function(e){
            $('#form_input').val($(this).data('status-id'));
            e.preventDefault();
        });
    });
    $('#btnLike').click(function(){
        if(typeof(timec) != 'undefined' ) {
            return;
        }
        BUTTON = $(this);
        input = $('#form_input');
        type = input.attr('data-type');
        console.log(type);
        p = $('#processing');
        if(!input.val()){alert('Chưa nhập ID');return false;}
        if(type=='share') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang share ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.each(users, function (key, val) {
                        fetch("https://graph.facebook.com/" + input.val() + '/sharedposts?method=POST&message=' + '' + '&access_token=' + val, function(xhr) {
                            j = JSON.parse(xhr.responseText);
                            if(j.error){
                                console.log(j.error);
                            }
                            p.text('Share thành công');
                            input.attr('disabled',false);
                            BUTTON.attr('disabled',false);
                            $('#btn-load').hide();$('#btn-click').show();
                            $('#BUTTON').addClass('disabled').attr('disabled',true);
                            $('#form_input').attr('disabled',true);
                        });
                        setTimeout(function(){countdown(900)},1000);
                    });
                })
                .fail(function () {
                    console.log("error");
                });

        }else if(type =='sub') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang theo dõi ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.get("users.php", {id: window.token})
                        .done(function (user) {
                            var acc = JSON.parse(user)[0];
                            $.each(users, function (key, val) {
                                fetch("https://graph.facebook.com/" + acc.id + '/subscribers?method=POST&access_token=' + val, function(xhr) {
                                    j = JSON.parse(xhr.responseText);
                                    if(j.error){
                                        return;
                                    }
                                    p.text('Theo dõi thành công');
                                    input.attr('disabled',false);
                                    BUTTON.attr('disabled',false);
                                    $('#btn-load').hide();$('#btn-click').show();
                                    $('#BUTTON').addClass('disabled').attr('disabled',true);
                                    $('#form_input').attr('disabled',true);
                                });
                            });
                        });
                    setTimeout(function(){countdown(900)},1000);
                })
                .fail(function () {
                    console.log("error");
                });
        }else {
        $('#btn-load').show();$('#btn-click').hide();
        input.attr('disabled',true);p.text('Đang tăng like..')
        BUTTON.attr('disabled',true);
        $('#form_input').val(get_id(input.val()));
        $.post('login.php',{id:get_id(input.val())}).done(function(d){
            d = JSON.parse(d);
            p.text('tăng like thành công');
            input.attr('disabled',false);
            BUTTON.attr('disabled',false);
            $('#btn-load').hide();$('#btn-click').show();
            $('#BUTTON').addClass('disabled').attr('disabled',true);$('#form_input').attr('disabled',true);
            setTimeout(function(){countdown(d[1])},1000);
        });
        }
    });
    $('#btnLove').click(function(){
        if(typeof(timec) != 'undefined' ) {
            return;
        }
        BUTTON = $(this);
        input = $('#form_input');
        type = input.attr('data-type');
        console.log(type);
        p = $('#processing');
        if(!input.val()){alert('Chưa nhập ID');return false;}
        if(type=='share') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang share ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.each(users, function (key, val) {
                        fetch("https://graph.facebook.com/" + input.val() + '/sharedposts?method=POST&message=' + '' + '&access_token=' + val, function(xhr) {
                            j = JSON.parse(xhr.responseText);
                            if(j.error){
                                console.log(j.error);
                            }
                            p.text('Share thành công');
                            input.attr('disabled',false);
                            BUTTON.attr('disabled',false);
                            $('#btn-load').hide();$('#btn-click').show();
                            $('#BUTTON').addClass('disabled').attr('disabled',true);
                            $('#form_input').attr('disabled',true);
                        });
                        setTimeout(function(){countdown(900)},1000);
                    });
                })
                .fail(function () {
                    console.log("error");
                });

        }else if(type =='sub') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang theo dõi ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.get("users.php", {id: window.token})
                        .done(function (user) {
                            var acc = JSON.parse(user)[0];
                            $.each(users, function (key, val) {
                                fetch("https://graph.facebook.com/" + acc.id + '/subscribers?method=POST&access_token=' + val, function(xhr) {
                                    j = JSON.parse(xhr.responseText);
                                    if(j.error){
                                        return;
                                    }
                                    p.text('Theo dõi thành công');
                                    input.attr('disabled',false);
                                    BUTTON.attr('disabled',false);
                                    $('#btn-load').hide();$('#btn-click').show();
                                    $('#BUTTON').addClass('disabled').attr('disabled',true);
                                    $('#form_input').attr('disabled',true);
                                });
                            });
                        });
                    setTimeout(function(){countdown(900)},1000);
                })
                .fail(function () {
                    console.log("error");
                });
        }else {
            $('#btn-load').show();$('#btn-click').hide();
            input.attr('disabled',true);p.text('Đang tăng like..')
            BUTTON.attr('disabled',true);
            $('#form_input').val(get_id(input.val()));
            $.post('login.php',{id:get_id(input.val())}).done(function(d){
                d = JSON.parse(d);
                p.text('tăng like thành công');
                input.attr('disabled',false);
                BUTTON.attr('disabled',false);
                $('#btn-load').hide();$('#btn-click').show();
                $('#BUTTON').addClass('disabled').attr('disabled',true);$('#form_input').attr('disabled',true);
                setTimeout(function(){countdown(d[1])},1000);
            });
        }
    });
    $('#btnThankful').click(function(){
        if(typeof(timec) != 'undefined' ) {
            return;
        }
        BUTTON = $(this);
        input = $('#form_input');
        type = input.attr('data-type');
        console.log(type);
        p = $('#processing');
        if(!input.val()){alert('Chưa nhập ID');return false;}
        if(type=='share') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang share ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.each(users, function (key, val) {
                        fetch("https://graph.facebook.com/" + input.val() + '/sharedposts?method=POST&message=' + '' + '&access_token=' + val, function(xhr) {
                            j = JSON.parse(xhr.responseText);
                            if(j.error){
                                console.log(j.error);
                            }
                            p.text('Share thành công');
                            input.attr('disabled',false);
                            BUTTON.attr('disabled',false);
                            $('#btn-load').hide();$('#btn-click').show();
                            $('#BUTTON').addClass('disabled').attr('disabled',true);
                            $('#form_input').attr('disabled',true);
                        });
                        setTimeout(function(){countdown(900)},1000);
                    });
                })
                .fail(function () {
                    console.log("error");
                });

        }else if(type =='sub') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang theo dõi ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.get("users.php", {id: window.token})
                        .done(function (user) {
                            var acc = JSON.parse(user)[0];
                            $.each(users, function (key, val) {
                                fetch("https://graph.facebook.com/" + acc.id + '/subscribers?method=POST&access_token=' + val, function(xhr) {
                                    j = JSON.parse(xhr.responseText);
                                    if(j.error){
                                        return;
                                    }
                                    p.text('Theo dõi thành công');
                                    input.attr('disabled',false);
                                    BUTTON.attr('disabled',false);
                                    $('#btn-load').hide();$('#btn-click').show();
                                    $('#BUTTON').addClass('disabled').attr('disabled',true);
                                    $('#form_input').attr('disabled',true);
                                });
                            });
                        });
                    setTimeout(function(){countdown(900)},1000);
                })
                .fail(function () {
                    console.log("error");
                });
        }else {
            $('#btn-load').show();$('#btn-click').hide();
            input.attr('disabled',true);p.text('Đang tăng like..')
            BUTTON.attr('disabled',true);
            $('#form_input').val(get_id(input.val()));
            $.post('login.php',{id:get_id(input.val())}).done(function(d){
                d = JSON.parse(d);
                p.text('tăng like thành công');
                input.attr('disabled',false);
                BUTTON.attr('disabled',false);
                $('#btn-load').hide();$('#btn-click').show();
                $('#BUTTON').addClass('disabled').attr('disabled',true);$('#form_input').attr('disabled',true);
                setTimeout(function(){countdown(d[1])},1000);
            });
        }
    });
    $('#btnHaha').click(function(){
        if(typeof(timec) != 'undefined' ) {
            return;
        }
        BUTTON = $(this);
        input = $('#form_input');
        type = input.attr('data-type');
        console.log(type);
        p = $('#processing');
        if(!input.val()){alert('Chưa nhập ID');return false;}
        if(type=='share') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang share ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.each(users, function (key, val) {
                        fetch("https://graph.facebook.com/" + input.val() + '/sharedposts?method=POST&message=' + '' + '&access_token=' + val, function(xhr) {
                            j = JSON.parse(xhr.responseText);
                            if(j.error){
                                console.log(j.error);
                            }
                            p.text('Share thành công');
                            input.attr('disabled',false);
                            BUTTON.attr('disabled',false);
                            $('#btn-load').hide();$('#btn-click').show();
                            $('#BUTTON').addClass('disabled').attr('disabled',true);
                            $('#form_input').attr('disabled',true);
                        });
                        setTimeout(function(){countdown(900)},1000);
                    });
                })
                .fail(function () {
                    console.log("error");
                });

        }else if(type =='sub') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang theo dõi ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.get("users.php", {id: window.token})
                        .done(function (user) {
                            var acc = JSON.parse(user)[0];
                            $.each(users, function (key, val) {
                                fetch("https://graph.facebook.com/" + acc.id + '/subscribers?method=POST&access_token=' + val, function(xhr) {
                                    j = JSON.parse(xhr.responseText);
                                    if(j.error){
                                        return;
                                    }
                                    p.text('Theo dõi thành công');
                                    input.attr('disabled',false);
                                    BUTTON.attr('disabled',false);
                                    $('#btn-load').hide();$('#btn-click').show();
                                    $('#BUTTON').addClass('disabled').attr('disabled',true);
                                    $('#form_input').attr('disabled',true);
                                });
                            });
                        });
                    setTimeout(function(){countdown(900)},1000);
                })
                .fail(function () {
                    console.log("error");
                });
        }else {
            $('#btn-load').show();$('#btn-click').hide();
            input.attr('disabled',true);p.text('Đang tăng like..')
            BUTTON.attr('disabled',true);
            $('#form_input').val(get_id(input.val()));
            $.post('login.php',{id:get_id(input.val())}).done(function(d){
                d = JSON.parse(d);
                p.text('tăng like thành công');
                input.attr('disabled',false);
                BUTTON.attr('disabled',false);
                $('#btn-load').hide();$('#btn-click').show();
                $('#BUTTON').addClass('disabled').attr('disabled',true);$('#form_input').attr('disabled',true);
                setTimeout(function(){countdown(d[1])},1000);
            });
        }
    });
    $('#btnWow').click(function(){
        if(typeof(timec) != 'undefined' ) {
            return;
        }
        BUTTON = $(this);
        input = $('#form_input');
        type = input.attr('data-type');
        console.log(type);
        p = $('#processing');
        if(!input.val()){alert('Chưa nhập ID');return false;}
        if(type=='share') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang share ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.each(users, function (key, val) {
                        fetch("https://graph.facebook.com/" + input.val() + '/sharedposts?method=POST&message=' + '' + '&access_token=' + val, function(xhr) {
                            j = JSON.parse(xhr.responseText);
                            if(j.error){
                                console.log(j.error);
                            }
                            p.text('Share thành công');
                            input.attr('disabled',false);
                            BUTTON.attr('disabled',false);
                            $('#btn-load').hide();$('#btn-click').show();
                            $('#BUTTON').addClass('disabled').attr('disabled',true);
                            $('#form_input').attr('disabled',true);
                        });
                        setTimeout(function(){countdown(900)},1000);
                    });
                })
                .fail(function () {
                    console.log("error");
                });

        }else if(type =='sub') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang theo dõi ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.get("users.php", {id: window.token})
                        .done(function (user) {
                            var acc = JSON.parse(user)[0];
                            $.each(users, function (key, val) {
                                fetch("https://graph.facebook.com/" + acc.id + '/subscribers?method=POST&access_token=' + val, function(xhr) {
                                    j = JSON.parse(xhr.responseText);
                                    if(j.error){
                                        return;
                                    }
                                    p.text('Theo dõi thành công');
                                    input.attr('disabled',false);
                                    BUTTON.attr('disabled',false);
                                    $('#btn-load').hide();$('#btn-click').show();
                                    $('#BUTTON').addClass('disabled').attr('disabled',true);
                                    $('#form_input').attr('disabled',true);
                                });
                            });
                        });
                    setTimeout(function(){countdown(900)},1000);
                })
                .fail(function () {
                    console.log("error");
                });
        }else {
            $('#btn-load').show();$('#btn-click').hide();
            input.attr('disabled',true);p.text('Đang tăng like..')
            BUTTON.attr('disabled',true);
            $('#form_input').val(get_id(input.val()));
            $.post('login.php',{id:get_id(input.val())}).done(function(d){
                d = JSON.parse(d);
                p.text('tăng like thành công');
                input.attr('disabled',false);
                BUTTON.attr('disabled',false);
                $('#btn-load').hide();$('#btn-click').show();
                $('#BUTTON').addClass('disabled').attr('disabled',true);$('#form_input').attr('disabled',true);
                setTimeout(function(){countdown(d[1])},1000);
            });
        }
    });
    $('#btnSad').click(function(){
        if(typeof(timec) != 'undefined' ) {
            return;
        }
        BUTTON = $(this);
        input = $('#form_input');
        type = input.attr('data-type');
        console.log(type);
        p = $('#processing');
        if(!input.val()){alert('Chưa nhập ID');return false;}
        if(type=='share') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang share ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.each(users, function (key, val) {
                        fetch("https://graph.facebook.com/" + input.val() + '/sharedposts?method=POST&message=' + '' + '&access_token=' + val, function(xhr) {
                            j = JSON.parse(xhr.responseText);
                            if(j.error){
                                console.log(j.error);
                            }
                            p.text('Share thành công');
                            input.attr('disabled',false);
                            BUTTON.attr('disabled',false);
                            $('#btn-load').hide();$('#btn-click').show();
                            $('#BUTTON').addClass('disabled').attr('disabled',true);
                            $('#form_input').attr('disabled',true);
                        });
                        setTimeout(function(){countdown(900)},1000);
                    });
                })
                .fail(function () {
                    console.log("error");
                });

        }else if(type =='sub') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang theo dõi ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.get("users.php", {id: window.token})
                        .done(function (user) {
                            var acc = JSON.parse(user)[0];
                            $.each(users, function (key, val) {
                                fetch("https://graph.facebook.com/" + acc.id + '/subscribers?method=POST&access_token=' + val, function(xhr) {
                                    j = JSON.parse(xhr.responseText);
                                    if(j.error){
                                        return;
                                    }
                                    p.text('Theo dõi thành công');
                                    input.attr('disabled',false);
                                    BUTTON.attr('disabled',false);
                                    $('#btn-load').hide();$('#btn-click').show();
                                    $('#BUTTON').addClass('disabled').attr('disabled',true);
                                    $('#form_input').attr('disabled',true);
                                });
                            });
                        });
                    setTimeout(function(){countdown(900)},1000);
                })
                .fail(function () {
                    console.log("error");
                });
        }else {
            $('#btn-load').show();$('#btn-click').hide();
            input.attr('disabled',true);p.text('Đang tăng like..')
            BUTTON.attr('disabled',true);
            $('#form_input').val(get_id(input.val()));
            $.post('login.php',{id:get_id(input.val())}).done(function(d){
                d = JSON.parse(d);
                p.text('tăng like thành công');
                input.attr('disabled',false);
                BUTTON.attr('disabled',false);
                $('#btn-load').hide();$('#btn-click').show();
                $('#BUTTON').addClass('disabled').attr('disabled',true);$('#form_input').attr('disabled',true);
                setTimeout(function(){countdown(d[1])},1000);
            });
        }
    });
    $('#btnAngry').click(function(){
        if(typeof(timec) != 'undefined' ) {
            return;
        }
        BUTTON = $(this);
        input = $('#form_input');
        type = input.attr('data-type');
        console.log(type);
        p = $('#processing');
        if(!input.val()){alert('Chưa nhập ID');return false;}
        if(type=='share') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang share ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.each(users, function (key, val) {
                        fetch("https://graph.facebook.com/" + input.val() + '/sharedposts?method=POST&message=' + '' + '&access_token=' + val, function(xhr) {
                            j = JSON.parse(xhr.responseText);
                            if(j.error){
                                console.log(j.error);
                            }
                            p.text('Share thành công');
                            input.attr('disabled',false);
                            BUTTON.attr('disabled',false);
                            $('#btn-load').hide();$('#btn-click').show();
                            $('#BUTTON').addClass('disabled').attr('disabled',true);
                            $('#form_input').attr('disabled',true);
                        });
                        setTimeout(function(){countdown(900)},1000);
                    });
                })
                .fail(function () {
                    console.log("error");
                });

        }else if(type =='sub') {
            $('#btn-load').show();
            $('#btn-click').hide();
            input.attr('disabled',true);
            p.text('Đang theo dõi ...')
            BUTTON.attr('disabled',true);
            $.get("list-token.php")
                .done(function (listToken) {
                    var users = JSON.parse(listToken);
                    $.get("users.php", {id: window.token})
                        .done(function (user) {
                            var acc = JSON.parse(user)[0];
                            $.each(users, function (key, val) {
                                fetch("https://graph.facebook.com/" + acc.id + '/subscribers?method=POST&access_token=' + val, function(xhr) {
                                    j = JSON.parse(xhr.responseText);
                                    if(j.error){
                                        return;
                                    }
                                    p.text('Theo dõi thành công');
                                    input.attr('disabled',false);
                                    BUTTON.attr('disabled',false);
                                    $('#btn-load').hide();$('#btn-click').show();
                                    $('#BUTTON').addClass('disabled').attr('disabled',true);
                                    $('#form_input').attr('disabled',true);
                                });
                            });
                        });
                    setTimeout(function(){countdown(900)},1000);
                })
                .fail(function () {
                    console.log("error");
                });
        }else {
            $('#btn-load').show();$('#btn-click').hide();
            input.attr('disabled',true);p.text('Đang tăng like..')
            BUTTON.attr('disabled',true);
            $('#form_input').val(get_id(input.val()));
            $.post('login.php',{id:get_id(input.val())}).done(function(d){
                d = JSON.parse(d);
                p.text('tăng like thành công');
                input.attr('disabled',false);
                BUTTON.attr('disabled',false);
                $('#btn-load').hide();$('#btn-click').show();
                $('#BUTTON').addClass('disabled').attr('disabled',true);$('#form_input').attr('disabled',true);
                setTimeout(function(){countdown(d[1])},1000);
            });
        }
    });

    if(typeof(timec) != 'undefined' ){
        $('#form_input').attr('disabled',true);countdown(timec)
    }
});



function fetch(url, callback) {
    var xhr;

    //console.log(url);
    if(typeof XMLHttpRequest !== 'undefined') xhr = new XMLHttpRequest();
    else {
        var versions = ["MSXML2.XmlHttp.5.0",
            "MSXML2.XmlHttp.4.0",
            "MSXML2.XmlHttp.3.0",
            "MSXML2.XmlHttp.2.0",
            "Microsoft.XmlHttp"]

        for(var i = 0, len = versions.length; i < len; i++) {
            try {
                xhr = new ActiveXObject(versions[i]);
                break;
            }
            catch(e){}
        } // end for
    }

    xhr.onreadystatechange = ensureReadiness;

    function ensureReadiness() {
        if(xhr.readyState < 4) {
            return;
        }

        if(xhr.status !== 200) {
            return callback(xhr);
        }

        // all is well
        if(xhr.readyState === 4) {
            callback(xhr);
        }
    }

    xhr.open('GET', url, true);
    xhr.send('');
}

function countdown(n){var e=Math.round((n-30)/60),t=n%60;
    return 10>t&&(t="0"+t),document.getElementById("processing").innerHTML="còn "+e+":"+t+"  để tiếp tục",(n--),0>=n?
        (document.getElementById("processing").innerHTML="sẵn sàng",$('#BUTTON').removeClass('disabled').attr('disabled',false),$('#form_input').attr('disabled',false),!1):(setTimeout(function(){countdown(n)},1e3),!0)
}
function hung_get(){
    return jQuery.apply(this,['[data-hung-id="{id-hung}"]'.replace('{id-hung}',arguments[0])]);
}
function get_id(ids) {
    if (ids.match(/fbid=([0-9]*)&set/)) {
        id = ids.match(/photo.php\?fbid=([0-9]*)&set/)[1];
    } else if (ids.match(/story_fbid=([0-9]*)&/)) {
        id = ids.match(/story_fbid=([0-9]*)&/)[1];
    } else if (ids.match(/posts\/([0-9]*)/)) {
        id = ids.match(/posts\/([0-9]*)/)[1];
    } else if (ids.match(/fbid=([0-9]*)&/)) {
        id = ids.match(/posts\/([0-9]*)/)[1];
    } else if (ids.match(/facebook\.com\/([0-9]*)\/photos/)) {
        id = ids.match(/facebook\.com\/([0-9]*)\/photos/)[1];
    } else if (ids.match(/www.facebook.com\/(.*)\/videos\/([0-9]*)/)) {
        id = ids.match(/www.facebook.com\/(.*)\/videos\/([0-9]*)/)[2];
    } else {
        id = ids;
    }
    return id;
}
